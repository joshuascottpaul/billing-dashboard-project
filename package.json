{
  "name": "billing-dashboard-project",
  "private": true,
  "version": "0.1.0",
  "description": "Reproducible billing analytics dashboard test harness",
  "scripts": {
    "test:e2e": "npx playwright test",
    "test:e2e:query": "npx playwright test dashboard/tests/e2e/query-args.spec.js",
    "test:e2e:headed": "npx playwright test --headed",
    "test:e2e:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.2"
  }
}
